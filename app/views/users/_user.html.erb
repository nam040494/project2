<li>
  <%= gravatar_for user, size: Settings.users.user.size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user.is_user?(user) %>
    | <%= link_to t(".delete"), user, method: :delete,
    data: { confirm: t(".are_you_sure?") } %>
  <% end %>
</li>
