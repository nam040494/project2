<%= form_for [micropost, micropost.comments.build] do |f| %>
  <!--<%= f.label :content, current_user.name %> -->
  <%= f.text_field :content, placeholder: "Write a comment" %>
  <%= f.submit "Submit" %>
<% end %>
